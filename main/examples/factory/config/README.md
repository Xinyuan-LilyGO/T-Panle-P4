# Factory configuration

Place factory application configuration shared across the app, services, and UI
in this directory. Board-level pin and peripheral definitions remain in the
`t_panel_p4_bsp` component.
