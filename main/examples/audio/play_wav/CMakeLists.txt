list(APPEND EXAMPLE_SRCS "${CMAKE_CURRENT_LIST_DIR}/main.c")
list(APPEND EXAMPLE_INCLUDE_DIRS "${CMAKE_CURRENT_LIST_DIR}")
list(APPEND EXAMPLE_REQUIRES audio_codec heap t_panel_p4_bsp)
list(APPEND EXAMPLE_EMBED_FILES "${CMAKE_CURRENT_LIST_DIR}/sample-3s.wav")
